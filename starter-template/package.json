{
  "name": "YOUR_PROJECT_NAME",
  "version": "1.0.0",
  "description": "YOUR_PROJECT_DESCRIPTION",
  "private": true,
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "docker build --file Dockerfile --tag daostack_ganache .",
    "test": "./node_modules/.bin/truffle test",
    "start": "npm run webpack && node dist/app.js",
    "ganache": "npm explore @daostack/migration -- npm run ganache",
    "webpack": "npx webpack --config webpack.config.js",
    "truffle-migrate": "./node_modules/.bin/truffle migrate",
    "launch-local": "npm run start && rm -rf build && npm run truffle-migrate && node dist/app.js"
  },
  "keywords": [
    "DAO",
    "DAOStack",
    "Ethereum",
    "Blockchain"
  ],
  "author": "YOUR_NAME",
  "license": "YOUR_LICENSE",
  "dependencies": {
    "@daostack/client": "0.1.19",
    "@daostack/migration": "0.0.1-rc.19-v3",
    "dotenv": "^8.0.0",
    "ethereumjs-wallet": "^0.6.3",
    "express": "^4.16.3",
    "truffle": "^5.0.22",
    "truffle-hdwallet-provider": "^1.0.10"
  },
  "devDependencies": {
    "webpack": "^4.17.1",
    "webpack-cli": "^3.1.2"
  }
}
